%p= image_tag @pin.image.url(:medium)
%h1= @pin.title
%p= @pin.description
%p= @pin.user.email
= link_to "Back", pins_index_path
= link_to "Edit", edit_pin_path
= link_to "Delete", pin_path, method: :delete, data: { confirm: "Are you sure?" }